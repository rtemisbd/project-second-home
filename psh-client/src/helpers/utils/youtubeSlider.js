export const youtubeSlider = (projects) => ({
  perPage: 1,
  perMove: 1,
  pagination: true,
  arrows: false,
  // width: "100%",
  // height: "auto",
  drag: true,
  focus: "center",
  // arrows: projects?.length > 1 ? true : false,
});
