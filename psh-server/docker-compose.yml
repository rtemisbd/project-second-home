services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: psh-server
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    volumes:
      - uploads:/app/public/uploads

volumes:
  uploads:
